<link rel="stylesheet" href="{{ 'section-footer.css' | asset_url }}">
<script src="{{ 'section-footer.js' | asset_url }}" defer="defer"></script>

{% style %}
  .site-footer {
    position: relative;
    background-color: {{ section.settings.footer_bg_color | default: '#0b100c' }};
    {% if section.settings.background_image != blank %}
      background-image: url('{{ section.settings.background_image | image_url: width: 2200 }}');
      background-size: cover;
      background-position: center center;
      background-repeat: no-repeat;
    {% endif %}
  }

  {% if section.settings.background_image != blank and section.settings.overlay_opacity > 0 %}
    .site-footer::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, {{ section.settings.overlay_opacity | divided_by: 100.0 }});
      pointer-events: none;
      z-index: 1;
    }
    .site-footer__inner,
    .site-footer__bottom {
      position: relative;
      z-index: 2;
    }
  {% endif %}
{% endstyle %}

<footer class="site-footer" data-site-footer>
  <div class="site-footer__inner">
    <!-- Column 1: Brand Info -->
    <div class="site-footer__brand-col">
      <a href="{{ routes.root_url }}" class="site-footer__logo-link" aria-label="{{ shop.name | escape }} home">
        {% if section.settings.logo != blank %}
          {{ section.settings.logo | image_url: width: 300 | image_tag: class: 'site-footer__logo', alt: shop.name, loading: 'lazy' }}
        {% else %}
          <img class="site-footer__logo" src="{{ 'logo.png' | asset_url }}" alt="{{ shop.name | escape }}" loading="lazy">
        {% endif %}
      </a>
      <p class="site-footer__brand-text">
        {{ section.settings.about_text | default: 'Kishan Bazar, Welcome to our farm-fresh organic kitchen. We build wholesome pantry staples and marketing experiences that help brands stand out and scale confidently.' }}
      </p>
      <div class="site-footer__social-row">
        <a href="{{ section.settings.x_url | default: '#' }}" class="social-btn" aria-label="X (Twitter)">X</a>
        <a href="{{ section.settings.facebook_url | default: '#' }}" class="social-btn" aria-label="Facebook">f</a>
        <a href="{{ section.settings.linkedin_url | default: '#' }}" class="social-btn" aria-label="LinkedIn">in</a>
        <a href="{{ section.settings.instagram_url | default: '#' }}" class="social-btn" aria-label="Instagram">📷</a>
      </div>
    </div>

    <!-- Column 2: Explore -->
    <div class="site-footer__col" data-footer-col>
      <button type="button" class="site-footer__col-title site-footer__col-toggle" aria-expanded="false" data-footer-toggle>
        <span>Explore</span>
        <span class="accordion-icon" aria-hidden="true">+</span>
      </button>
      <ul class="site-footer__list" role="list">
        <li><a href="{{ routes.root_url }}">About</a></li>
        <li><a href="{{ routes.collections_url }}">Services</a></li>
        <li><a href="{{ routes.collections_url }}">Portfolios</a></li>
        <li><a href="{{ routes.root_url }}#contact">Contact</a></li>
        <li><a href="{{ routes.collections_url }}">Pricing</a></li>
        <li><a href="{{ routes.root_url }}">FAQ</a></li>
        <li><a href="{{ routes.root_url }}">Team Members</a></li>
      </ul>
    </div>

    <!-- Column 3: Policy -->
    <div class="site-footer__col" data-footer-col>
      <button type="button" class="site-footer__col-title site-footer__col-toggle" aria-expanded="false" data-footer-toggle>
        <span>Policy</span>
        <span class="accordion-icon" aria-hidden="true">+</span>
      </button>
      <ul class="site-footer__list" role="list">
        <li><a href="/policies/terms-of-service">Terms of Use</a></li>
        <li><a href="/policies/privacy-policy">Privacy Policy</a></li>
        <li><a href="/policies/refund-policy">Refund Policy</a></li>
        <li><a href="/policies/shipping-policy">Shipping Policy</a></li>
      </ul>
    </div>

    <!-- Column 4: Contact Us -->
    <div class="site-footer__col site-footer__contact-col" data-footer-col>
      <button type="button" class="site-footer__col-title site-footer__col-toggle" aria-expanded="false" data-footer-toggle>
        <span>Contact Us</span>
        <span class="accordion-icon" aria-hidden="true">+</span>
      </button>
      <div class="site-footer__contact-boxes">
        <a href="tel:{{ section.settings.phone | default: '+91 88888 88888' }}" class="contact-box">
          <span class="contact-box__icon">📞</span>
          <span class="contact-box__text">{{ section.settings.phone | default: '+91 88888 88888' }}</span>
        </a>
        <a href="mailto:{{ section.settings.email | default: 'support@kishanbazar.com' }}" class="contact-box">
          <span class="contact-box__icon">✉️</span>
          <span class="contact-box__text">{{ section.settings.email | default: 'support@kishanbazar.com' }}</span>
        </a>
      </div>
    </div>
  </div>

  <div class="site-footer__bottom">
    <p class="site-footer__copyright">Copyright &copy; {{ 'now' | date: '%Y' }}, {{ shop.name }}</p>
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "tag": "section",
  "class": "section-footer",
  "settings": [
    { "type": "image_picker", "id": "background_image", "label": "Footer Background Image" },
    { "type": "color", "id": "footer_bg_color", "label": "Footer Background Color", "default": "#0b100c" },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 90,
      "step": 5,
      "unit": "%",
      "label": "Background Dark Overlay Opacity",
      "default": 35
    },
    { "type": "image_picker", "id": "logo", "label": "Footer logo" },
    { "type": "textarea", "id": "about_text", "label": "About description", "default": "Kishan Bazar, Welcome to our farm-fresh organic kitchen. We build wholesome pantry staples and marketing experiences that help brands stand out and scale confidently." },
    { "type": "url", "id": "x_url", "label": "X (Twitter) URL" },
    { "type": "url", "id": "facebook_url", "label": "Facebook URL" },
    { "type": "url", "id": "linkedin_url", "label": "LinkedIn URL" },
    { "type": "url", "id": "instagram_url", "label": "Instagram URL" },
    { "type": "text", "id": "phone", "label": "Phone Number", "default": "+91 88888 88888" },
    { "type": "text", "id": "email", "label": "Email Address", "default": "support@kishanbazar.com" }
  ],
  "presets": [{ "name": "Footer" }]
}
{% endschema %}
